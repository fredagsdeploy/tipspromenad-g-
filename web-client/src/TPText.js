import React from "react";
import styled from "styled-components/native";

import { plaintextFont, baseTextColor } from "./config";

export default styled.Text`
  font-family: ${plaintextFont};
  color: ${baseTextColor};
`;
